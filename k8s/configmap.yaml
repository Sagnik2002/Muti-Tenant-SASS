apiVersion: v1
kind: ConfigMap
metadata:
  name: saas-workspace-config
  namespace: saas-workspace
data:
  NODE_ENV: "production"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "saas_workspace"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  FRONTEND_URL: "http://frontend-service"
